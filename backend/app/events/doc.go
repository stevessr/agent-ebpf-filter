// Package events — reserved for event processing pipeline extraction.
//
// Target files (from app/): context_event, contextutilsevent, envelope_event,
// event_flows, events_hooks, events_network, graph_execution, kernel_risk,
// kernel_risk_feedback, recording_event, alerts_semantic, etc.
//
// Prerequisites: resolve references to runtimeSettingsStore, broadcast,
// capturedEventArchive (need dependency injection or global registry).
package events
