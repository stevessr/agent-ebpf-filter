{
  "name": "@agent-ebpf-filter/bpf-ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test",
    "check": "bun run build && bun test",
    "compile:example": "bun src/cli.ts examples/exec.ts --out generated/exec.bpf.c --manifest generated/exec.json"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "typescript": "5.9.3"
  }
}
