# bpf-ts

Experimental TypeScript-like eBPF DSL compiler.

This directory is intentionally isolated from the production TLS capture path while the compiler ABI and verifier-aware semantics stabilize.
