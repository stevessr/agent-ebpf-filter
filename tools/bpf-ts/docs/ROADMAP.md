# Roadmap

1. Stabilize the restricted TypeScript AST and C backend.
2. Add CO-RE field access primitives and typed tracepoint contexts.
3. Generate loader bindings from the attach manifest.
4. Add verifier-log source mapping back to TypeScript spans.
5. Add a native eBPF instruction backend behind the same IR.
