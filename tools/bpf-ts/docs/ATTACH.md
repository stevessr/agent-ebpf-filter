# Attach model

Attach intent is part of IR and the versioned manifest. Kernel probes and tracepoints can encode targets in libbpf section names; uprobes retain their symbol in the manifest because executable resolution belongs to the userspace probe manager.
