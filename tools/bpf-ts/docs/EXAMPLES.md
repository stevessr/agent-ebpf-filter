# Examples

`examples/exec.ts` demonstrates tracepoint attachment, ring-buffer output, a hash counter and a bounded loop. `examples/tls-write.ts` demonstrates uprobe argument access and attach-manifest metadata for `SSL_write`.
