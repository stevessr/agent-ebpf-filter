# Integration boundary

Production integration should consume generated ELF plus the attach manifest from the existing Go probe manager. The compiler itself remains a build/tooling component and should not add a JavaScript runtime dependency to the deployed backend.
