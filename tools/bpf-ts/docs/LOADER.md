# Loader handoff

The initial manifest is deliberately minimal. A future Go loader generator can map each manifest probe to the existing `link.Kprobe`, executable `Uprobe`, or tracepoint attachment code and bind map names to generated object fields.
