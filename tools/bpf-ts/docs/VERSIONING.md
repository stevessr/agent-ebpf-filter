# Versioning

The attach manifest starts at schema version `1`. The DSL has no compatibility guarantee until the PoC graduates; breaking semantic changes should increment the manifest/schema version before production loader integration.
