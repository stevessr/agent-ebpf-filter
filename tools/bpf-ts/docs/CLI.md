# CLI

`bun src/cli.ts input.ts --out generated/program.bpf.c --manifest generated/program.json`

The command writes readable generated C and an optional machine-readable attach manifest. It does not load the BPF program.
