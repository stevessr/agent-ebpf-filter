# PoC graduation criteria

The first milestone is complete only when checked-in examples pass AST/IR unit tests and the emitted C compiles through clang's BPF backend in CI. Production loader integration is a separate milestone.
