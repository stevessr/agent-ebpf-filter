# Dependencies

The compiler package intentionally depends only on the TypeScript compiler API at runtime/build-tool level. Generated programs depend on the system clang/libbpf headers already used elsewhere in this repository.
