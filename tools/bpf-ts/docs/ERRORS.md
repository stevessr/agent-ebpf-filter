# Errors

Unsupported language constructs fail at the TypeScript source location. Unknown helper/map calls are errors rather than pass-through C. This is deliberate so adding a new DSL operation always requires an explicit lowering implementation.
