# Build

Install the isolated tool dependencies, run the compiler tests, compile an example to `.bpf.c`, then invoke clang with the BPF target and architecture define. CI performs this sequence without changing the deployed backend runtime.
