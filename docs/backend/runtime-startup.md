# 后端启动链路

当前后端主入口位于 `backend/app/main.go`。这点很重要：部分历史文档仍写 `backend/main.go`，维护时应以当前代码为准。

## Main() 启动顺序

```mermaid
flowchart TD
    Start([Main Entry]) --> Bootstrap{isBootstrapMode?}
    Bootstrap -->|Yes| BootstrapMaps[bootstrapTrackerMaps]
    BootstrapMaps --> Exit([Exit])
    
    Bootstrap -->|No| Privileges[ensureBackendPrivileges]
    Privileges -->|Need sudo| Restart[sudo/pkexec restart]
    Restart --> Start
    
    Privileges -->|OK| Hooks[refreshHooksPaths]
    Hooks --> LoadSettings[runtimeSettingsStore.LoadOrCreate]
    LoadSettings --> KillPrev[killPreviousBackendProcesses]
    KillPrev --> LoadMaps[ensureTrackerMapsLoaded]
    
    LoadMaps --> Features[newFeatureRegistry]
    Features --> DomainFwd{domain forward?}
    DomainFwd -->|Yes| ActivateFwd[domainForwardProxyService.Activate]
    DomainFwd -->|No| TLSSetup
    ActivateFwd --> TLSSetup[TLS runtime setup]
    
    TLSSetup --> Ringbuf[ringbuf.NewReader]
    Ringbuf --> EventReader[startKernelEventReader]
    EventReader --> BgJobs[startRuntimeBackgroundJobs]
    
    BgJobs --> Sandbox[ApplySandbox]
    Sandbox --> Gin[gin.Default]
    Gin --> Gateway[clusterGatewayMiddleware]
    Gateway --> Eviction[startArchiveEvictionLoop]
    
    Eviction --> Routes[registerRoutes]
    Routes --> Seed[seedDefaultTrackedCommands]
    Seed --> Port[chooseBackendPort]
    Port --> WritePort[configureRuntimePort]
    
    WritePort --> ML{ML feature?}
    ML -->|Yes| StartML[startDeferredMLRuntime]
    ML -->|No| Plugins
    StartML --> Plugins{plugins feature?}
    Plugins -->|Yes| StartPlugins[startDeferredPluginRuntime]
    Plugins -->|No| Run
    StartPlugins --> Run[r.Run :port]
    Run --> Serve([HTTP Server Running])
    
    style Start fill:#bfb
    style Serve fill:#bfb
    style Privileges fill:#fbb
    style LoadMaps fill:#bbf
    style EventReader fill:#bbf
    style Run fill:#bfb
```

## Bootstrap 与特权

```mermaid
sequenceDiagram
    participant User as User
    participant Main as Main()
    participant Check as ensureBackendPrivileges
    participant Sudo as sudo/pkexec
    participant eBPF as eBPF Loader
    participant BPFfs as /sys/fs/bpf
    
    User->>Main: ./agent-ebpf-filter
    Main->>Check: check capabilities
    
    alt has CAP_BPF + CAP_NET_ADMIN
        Check-->>Main: OK
    else missing capabilities
        Check->>Sudo: re-exec with elevation
        Sudo->>User: prompt password
        User-->>Sudo: authenticated
        Sudo->>Main: restart as privileged
        Main->>Check: check again
        Check-->>Main: OK
    end
    
    Main->>eBPF: load tracker/cgroup/LSM objects
    eBPF->>BPFfs: pin maps to /sys/fs/bpf/agent-ebpf/
    eBPF->>BPFfs: pin links
    eBPF->>BPFfs: set restrictive permissions
    BPFfs-->>Main: ready
```

启动最先处理两件事：

1. bootstrap mode：只初始化 eBPF components 后退出；
2. 特权检查：如果当前进程能力不足，后端可通过 `sudo` / `pkexec` 自提权重启。

需要特权的原因：

- 加载 eBPF object；
- attach tracepoint / cgroup / LSM；
- pin maps / links；
- 可选绑定 80/443；
- 管理 bpffs 下的 restrictive map permissions。

## Runtime settings 加载

`runtimeSettingsStore.LoadOrCreate()` 会加载或创建运行时配置，默认位置：

```bash
~/.config/agent-ebpf-filter/runtime.json
```

典型配置结构：

```json
{
  "log_persistence_enabled": true,
  "log_file_path": "~/.config/agent-ebpf-filter/events.jsonl",
  "access_token": "<auto-generated-uuid>",
  "max_event_count": 10000,
  "max_event_age": "24h",
  "shell_sessions_enabled": false,
  "system_run_enabled": false,
  "hook_management_enabled": false,
  "policy_management_enabled": false,
  "otlp_enabled": false,
  "otlp_endpoint": "http://localhost:4318/v1/traces",
  "otlp_service_name": "agent-ebpf-filter",
  "tls_capture_enabled": false,
  "ml_config": {},
  "kernel_risk_feedback": {
    "enabled": false,
    "min_risk_score": 0.7
  }
}
```

配置会影响：

- auth token；
- event archive 大小和过期时间；
- JSONL persistence；
- shell sessions；
- system run；
- hook management；
- policy management；
- TLS capture；
- OTLP；
- ML；
- domain forward；
- kernel risk feedback。

## eBPF 初始化

```mermaid
graph TB
    Load[ensureTrackerMapsLoaded] --> Check{maps pinned?}
    Check -->|Yes| Open[Open existing maps]
    Check -->|No| Create[Load eBPF objects]
    
    Create --> Pin[Pin maps/links]
    Pin --> Perms[Set permissions]
    
    Open --> Main[agent_pids<br/>tracked_comms<br/>tracked_paths<br/>events ringbuf]
    Perms --> Main
    
    Main --> Cgroup{cgroup feature?}
    Cgroup -->|Yes| CgroupMaps[cgroup_blocked_ips<br/>cgroup_blocked_ports<br/>cgroup_sandbox_pids]
    Cgroup -->|No| LSM
    CgroupMaps --> LSM
    
    LSM{LSM feature?}
    LSM -->|Yes| LSMMaps[lsm_blocked_exec_paths<br/>lsm_blocked_file_names]
    LSM -->|No| Ready
    LSMMaps --> Ready[Maps Ready]
    
    style Main fill:#bbf
    style CgroupMaps fill:#fbb
    style LSMMaps fill:#fbb
    style Ready fill:#bfb
```

`ensureTrackerMapsLoaded()` 负责主 tracker maps 和 links 的加载 / 打开 / pinning。

主 pin 路径：

```text
/sys/fs/bpf/agent-ebpf/maps
/sys/fs/bpf/agent-ebpf/links
```

cgroup 与 LSM 有各自子目录：

```text
/sys/fs/bpf/agent-ebpf/cgroup_sandbox/maps
/sys/fs/bpf/agent-ebpf/cgroup_sandbox/links
/sys/fs/bpf/agent-ebpf/lsm_enforcer/maps
/sys/fs/bpf/agent-ebpf/lsm_enforcer/links
```

## 后台任务

`startRuntimeBackgroundJobs(features)` 启动：

```go
// 伪代码示例
func startRuntimeBackgroundJobs(features FeatureRegistry) {
    // 核心事件广播
    go startEventBroadcaster(ctx, broadcastChan)
    
    // Wrapper UDS 策略服务器
    go startWrapperUDSServer(ctx, "/tmp/agent-ebpf.sock")
    
    // 可选：内核风险反馈闭环
    if features.IsEnabled("kernel_risk_feedback") {
        go startKernelRiskFeedbackWorker(ctx)
    }
    
    // 网络层 GC
    go startCgroupAttributionGC(ctx)
    go startDNSCacheGC(ctx)
    go startTCPStateTrackerGC(ctx)
    go startFlowAggregatorGC(ctx)
    
    // 可选：外泄检测
    go startExfilDetectionLoop(ctx)
    
    // GeoIP 初始化
    go initGeoIPDatabase(ctx)
    
    // 可选：cgroup / LSM 加载
    if features.IsEnabled("sandbox_cgroup") {
        go loadCgroupSandbox(ctx)
    }
    if features.IsEnabled("sandbox_lsm") {
        go loadLSMEnforcer(ctx)
    }
}
```

- event broadcaster.
- kernel risk feedback worker.
- UDS server.
- cgroup attribution GC.
- DNS cache GC.
- TCP state tracker GC.
- flow aggregator GC.
- exfil detection loop.
- GeoIP init.
- optional cgroup sandbox loader.
- optional LSM enforcer loader.

## 端口选择

后端会在 `8080..8089` 选择可用端口，随后写入运行时端口 handoff 文件。前端 Vite dev proxy、adapters 和 hook endpoint 推导会依赖该端口。

```go
// 伪代码
func chooseBackendPort() int {
    for port := 8080; port <= 8089; port++ {
        if isPortAvailable(port) {
            return port
        }
    }
    panic("no available port in 8080-8089")
}

func configureRuntimePort(port int) {
    // 写入 backend/.port 供前端 dev proxy 和 adapters 读取
    os.WriteFile("backend/.port", []byte(strconv.Itoa(port)), 0644)
}
```

---

## 相关导航

- [构建与运行](../operations/build-and-run.md)
- [运行时边界](../architecture/runtime-boundaries.md)
- [Runtime Settings 与 Feature Manifest](runtime-settings-features.md)
- [路由与 API](routes-api.md)
- [代码入口索引](../reference/code-entrypoints.md)
