server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/agent-ebpf-promtail-positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: agent-ebpf-agentsight
    static_configs:
      - targets: [localhost]
        labels:
          job: agent-ebpf-agentsight
          __path__: /var/log/agent-ebpf/events.jsonl
    pipeline_stages:
      - json:
          expressions:
            receivedAt: receivedAt
            event: event
      - labels:
          receivedAt:
