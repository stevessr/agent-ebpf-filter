name: Repository integrity

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  gitlinks:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Validate gitlink metadata
        run: scripts/check-gitlinks.sh
