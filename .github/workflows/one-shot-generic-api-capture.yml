name: one-shot generic API capture staging

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'staging workflow is reconfigured by the next capture phase'
